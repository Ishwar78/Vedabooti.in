import React,{useState} from "react";import AdminShell from "../../components/AdminShell";import "./AdminCategories.css";
export default function AdminCategories(){const [show,setShow]=useState(false);const [search,setSearch]=useState("");const rows=[["Immunity Boosters","12","Active"],["Skin Care","10","Active"],["Hair Care","8","Active"],["Digestive Health","9","Active"],["Herbal Teas","6","Active"],["Wellness Packs","5","Draft"]];return <AdminShell><div className="crud-head"><div><span className="eyebrow">Admin Management</span><h1>Categories</h1><p>Manage your categories with a focused dedicated workspace.</p></div><button className="btn" onClick={()=>setShow(v=>!v)}>Add New</button></div>{show&&<form className="crud-form" onSubmit={e=>{e.preventDefault();setShow(false)}}><input className="input" placeholder="Name / title" required/><input className="input" placeholder="Status or category"/><input className="input" placeholder="Short description"/><button className="btn">Save</button></form>}<div className="crud-toolbar"><input className="input" placeholder="Search..." value={search} onChange={e=>setSearch(e.target.value)}/><select className="select"><option>All Status</option><option>Active</option><option>Pending</option></select></div><div className="crud-table"><div className="table-row table-head"><span key="Category">Category</span><span key="Products">Products</span><span key="Status">Status</span><span key="Actions">Actions</span></div>{rows.filter(r=>r.join(" ").toLowerCase().includes(search.toLowerCase())).map((r,i)=><div className="table-row" key={i}>{r.map((c,j)=><span key={j}>{c}</span>)}<div className="row-actions"><button>Edit</button><button>Delete</button></div></div>)}</div></AdminShell>}