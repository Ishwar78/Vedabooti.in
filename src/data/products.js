// Live products are fetched dynamically from MongoDB (/api/products)
// All static mock products removed as requested

export const allProducts = [];
export const homeProducts = [];

export const productMap = {};

export function getProductBySlug(slug) {
    return null;
}
